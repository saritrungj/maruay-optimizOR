namespace optimizOR.Models
{
    public enum LogLevel
    {
        Info,
        Success,
        Warning,
        Error
    }

    public enum TweakType
    {
        Registry,
        IniFile,
        Debloat
    }
}

